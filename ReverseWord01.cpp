#include "bits/stdc++.h"
using namespace std;
int main(){
    string S = i.like.this.program.very.much;
    S = input.split(S);
    int n=S.length();
    string rev = "";
    for(int i=n-1; i>=0; i--){
        rev+=S[i];
    } 
    return 0;  
}
